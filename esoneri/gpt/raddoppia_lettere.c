#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <ctype.h>

/*
 * ESERCIZIO:
 * Scrivere una funzione `char* raddoppiaLettere(char* str)` che, data una stringa,
 * restituisce una nuova stringa in cui ogni lettera alfabetica è duplicata (ripetuta due volte),
 * mentre tutti gli altri caratteri restano invariati.
 *
 * Esempio:
 * Input:  "ciao!"
 * Output: "ccii-aaoo!"
 *
 * - Usare malloc per allocare la nuova stringa.
 * - Usare isalpha() per verificare le lettere.
 */

int main() {
    // Da completare
    return 0;
}
