#include <stdio.h>
#include <stdlib.h>
#include <string.h>

/*
 * ESERCIZIO:
 * Scrivere una funzione `char* unisciInvertendoSeconda(char* s1, char* s2)` che,
 * date due stringhe `s1` e `s2`, restituisce una nuova stringa formata dalla
 * concatenazione di `s1` + (s2 invertita).
 *
 * Esempio:
 * s1 = "hello", s2 = "world" → ritorna "hellodlrow"
 *
 * - Usare malloc per creare la nuova stringa.
 * - Gestire i casi NULL restituendo NULL.
 */

int main() {
    // Da completare
    return 0;
}
